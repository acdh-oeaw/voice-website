---
interface Props {
	class?: string;
	search?: "exclude";
}

const { class: className, search } = Astro.props;
---

<main
	class:list={["isolate bg-neutral-50", className]}
	id="main-content"
	data-pagefind-body={search === "exclude" ? undefined : ""}
	tabindex={-1}
>
	<slot />
</main>
